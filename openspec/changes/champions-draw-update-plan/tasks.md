# Tasks: Champions Draw — UI/UX Update

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 4500 (range 3500–5500) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | 6 work units (below) |
| Delivery strategy | single-pr — **`size:exception` APPROVED by maintainer 2026-09-17** |
| Chain strategy | not applicable (single PR; `feature-branch-chain` recorded as a preference only if delivery later splits) |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High
Estimated changed lines: 4500

Pure-move extraction (`main.jsx` → `views/**`) inflates the raw count; excluding move noise, authored change is still ~2500+ lines. Both exceed 400. Under `single-pr`, the orchestrator must obtain `size:exception` or a chain decision before apply. Ordering below is also the safe landing order: each unit reverts alone.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 1 | Tokens, theme, dead CSS, breakpoints, lib extraction | PR 1 | `npm run build` | browser 390/1440 both themes, reload persistence | `styles.css` token blocks, `lib/*.js`, `index.html` script |
| 2 | Primitives + duplicate collapse | PR 2 | `npm run build` + `python manage.py test` | render each primitive with fixtures | `components/**` + adopters |
| 3 | Shell, mobile nav, view extraction | PR 3 | `npm run build` | keyboard pass 390/920/1120/1440, drawer trap | `views/**`, shell markup/CSS |
| 4 | States applied + accessibility | PR 4 | `npm run build` | force 500/502, Tab pass, both themes | state components + fetch-site edits |
| 5 | Career in shell | PR 5 | `npm run test:career` | load a legacy `champions_draw_career_v1` payload | `CareerApp.jsx` + `.career-*` CSS |
| 6 | Reconciliation | PR 6 | `node --test frontend/src/lib/reconcile.test.mjs` | offline + 502 in devtools while typing | `lib/reconcile.js`, `lib/useReconciliation.js` + call sites |

### Codes and execution notes

`DT` design-tokens · `AS` app-shell · `UC` ui-components · `US` ui-states · `A11Y` accessibility · `CS` career-shell-integration · `PR` prediction-reconciliation. Each code name is the spec requirement heading it maps to.
Invariants: `INV1` z3 draw POST + reveal `summary` · `INV2` reveal state machine · `INV3` prediction storage keys/shapes · `INV4` tiebreaker parity · `INV5` live-poll cadence + 502 · `INV6` match-detail focus return.
Commands run from the **repo root** (`package.json` and `vite.config.js` are at root; `frontend/` holds only `index.html` + `src/`). UI is invisible to Django until `npm run build` (→ `static/ui/`).

## Phase 1 — Foundations (tokens, theme, decomposition prerequisite)

- [x] 1.1 Move `groupBy` (main.jsx:67) verbatim to `frontend/src/lib/groupBy.js`; repoint `main.jsx:1131`, `RealDrawView.jsx:3`, `MatchdayScoreBoard.jsx:1`; drop the `main.jsx` export. AC UC:decomposition-prereq. ✓ `npm run build`; no view imports `main.jsx`. RB `lib/groupBy.js` + 3 import sites.
- [x] 1.2 Extract helpers: `apiFetch`/`getCookie` → `lib/api.js`; `shortTime`/`shortDay`/`inHomeRange` → `lib/format.js`; `normTeamName`/`toMiniRow` → `lib/teams.js`; delete `MatchDetailView.jsx:7` private twin. AC UC:decomposition-prereq; INV1 (shared POST path unchanged). ✓ `npm run build` + `python manage.py test`. RB 3 lib files + call sites.
- [x] 1.3 Token layer in `styles.css`: complete light `:root` + `[data-theme="dark"]`; space/radius/shadow/type/z scales; `--focus-ring-color`; status fills carry ink `#142033`; light `--muted` → `#5b6b82`. AC DT:dual-theme-token-layer, DT:complete-scales, DT:aa-contrast-both-themes. ✓ `getComputedStyle` per token in both themes + computed contrast. RB `styles.css` token blocks only.
- [x] 1.4 `lib/theme.js` (`getTheme`/`setTheme`/`applyTheme` over `champions_draw_theme`) + pre-paint `<head>` script in `frontend/index.html` (light default, sets `color-scheme`) + the switch control. AC DT:theme-switch, A11Y:keyboard-focus. ✓ reload persists with no wrong-theme flash; keyboard toggles; key ≠ protected keys. RB `lib/theme.js`, `index.html`, switch markup.
- [x] 1.5 Delete dead CSS (`.homepage-shell`, `.landing*`, `.auth-callout`, `.workspace-home-button`, `.product-hub*`, `.product-grid`, `.product-card*` + their media queries) and the `.product-card-career` repeating-gradient stripe. AC DT:dead-css-removed, UC:no-banned-decoration. ✓ dead-selector scan against markup/JS. RB `styles.css` deletions.
- [x] 1.6 Fix the breakpoint set `1120/920/760/640`; make every `@media` width a member and the shell threshold `920` appear once; nav mode stays media-query-driven (no JS remount). AC AS:declarative-breakpoint-scale. ✓ extract all `@media` widths → all members. RB media-query lines.

## Phase 2 — Primitives (dependency order)

- [x] 2.1 `components/Crest.jsx` on the `TeamLogo`/`TeamBadge` base, consistent sizes + initials fallback carrying the team name; replace `MatchDetailView` `Crest`, `RealDrawView` local `TeamLogo` + raw span (485), raw spans in `MatchdayScoreBoard`/`LeagueTable`/`PlayoffBracket`/`KnockoutBracket`/`PredictionApp:823`. AC UC:single-crest. ✓ build + crest-markup scan + missing-image render. RB `components/Crest.jsx` + adopters.
- [x] 2.2 `components/{Button,Badge,Metric}.jsx`: one definition per variant covering hover/focus/active/disabled; adopt or delete the dead `Metric` (main.jsx:1425); disabled never color-only. AC UC:shared-primitives, A11Y:44px-targets, A11Y:keyboard-focus. ✓ build + per-variant keyboard/disabled pass. RB 3 components + adoption.
- [x] 2.3 `components/SegmentControl.jsx` (arrow keys, announced selection) replacing `ViewTabs` (main.jsx:1443), `PredictionApp` sub-tabs and `.segment-control`. AC UC:single-segment-control, AS:keyboard-nav-not-color-only. ✓ build + arrow-key pass. RB component + 3 sites.
- [x] 2.4 `components/StandingsTable.jsx` owning row-qualified/playoffs/eliminated logic once, with non-color cues; replace `LeagueTable.jsx:14-61`, `RealDrawView.jsx:460-506`, `PredictionApp.jsx:805-835`, `main.jsx:568-614` and `409-450/802-846`; ordering and numbers unchanged. AC UC:single-standings-table; INV4. ✓ build + pre/post table diff at positions 1/9/30. RB component + 5 sites.
- [x] 2.5 `components/FixtureRow.jsx` (competition, kickoff, both crests, score-or-time, status text, tabular figures) replacing the five duplicated rows. AC UC:single-fixture-row. ✓ build + live vs scheduled render. RB component + 5 sites.
- [x] 2.6 `components/States.jsx`: skeleton at settled dimensions + `aria-busy`, empty with a next action, error naming the failure + per-request retry, `aria-live` announcement. AC US:loading-preserves-layout, US:empty-state-action, US:async-announced. ✓ isolated render with fixtures. RB `States.jsx` + `styles.css` `.skeleton*` rules (RB extended: the settled row metrics live in the stylesheet).

## Phase 3 — Shell and views

- [x] 3.1 One shell hosting `home|teams|career|real|workspace`: 240px sidebar, `.app-main` offset, `nav`/`main` landmarks, one `h1`-leading order per view, no competing page chrome. AC AS:one-shell, A11Y:skip-link-landmarks. ✓ `npm run build`; headless Chromium (Playwright, API stubbed to 500) — skip link is the first focusable, renders on first Tab, Enter moves focus to `main#main-content`; one `nav[aria-label="Primary"]` + one `main[aria-label="Main content"]`, zero nested mains; exactly one leading `h1` in each of the five views and in all six workspace tabs; 240px rail at 1440, nav absent below 920. RB shell markup + CSS.
- [x] 3.2 Mobile navigation: bottom bar (5 views) + `<dialog>` drawer (5 views + 6 workspace tabs), `aria-current` + non-color active marker; drawer trap coordinated with the match-detail overlay so Escape/focus never compete; reduced-motion appearance. AC AS:breakpoint-switching, AS:keyboard-nav-not-color-only, A11Y:reduced-motion-baseline. ✓ 390px keyboard pass; drawer open/close while the overlay is open; threshold crossing preserves view/tab/scroll. RB nav components + CSS.
- [x] 3.3 Extract views to `frontend/src/views/**` leaf-most: `TeamsBrowser`/`TeamPage`, `RealDrawView`, `Homepage`+`HomeMatchCard` (greeting + metric cards + live hub + quick actions only — no charts, no recent-draw section), `MatchDetailView` overlay, workspace panels, `InteractiveDraft`; no view imports `main.jsx`. AC UC:decomposition-prereq, AS:one-shell. ✓ build + import scan + homepage composition check. RB `views/**` + `main.jsx` removals.
- [x] 3.4 Reduce `main.jsx` to a composition root (`App` + `render`) with state-based view/tab wiring. AC UC:decomposition-prereq. ✓ build + `main.jsx` size + zero cycles. RB `main.jsx`.
- [x] 3.5 Prove the shell kept the invariants: z3 draw POST + `summary` shape, `revealStart`/`revealSession`/`pendingFinalize` (1553-1582), 60s hub refresh / 30s live poll, 502 contract, prediction keys/shapes. AC AS:shell-preserves-invariants; INV1/INV2/INV3/INV5. ✓ `python manage.py test` → `Ran 100 tests in 68.184s` / `OK`; HEAD→worktree diff: poll effects (36 lines) and `InteractiveDraft` verbatim moves, only `export default` differs; headless Chromium on built `static/ui/` (API stubbed): reveal continued 8→48/144 across a view switch and across drawer open/close, then reached `Fixtures` with exactly 1 draw POST (`summary` notice "ran 2025-26 with 144 fixtures"); interactive `pendingFinalize` still completed after the drawer open/close (1 pick POST); live timers `{30000:1, 60000:1}` at all 6 snapshots over 124s incl. 6 view switches, live requests per 30s window 1,2,1,1 (60s refresh re-arm +1 — stable, not unbounded); 502 → no page-level error, hub and nav usable; `localStorage` keys `champions_draw_theme`, `champions_draw_prediction_1_Ada`, `champions_draw_player_name` (pre-existing) — no new/renamed key. RB revert the offending Phase-3 unit.

## Phase 4 — States applied + accessibility

- [x] 4.1 Apply the four-state contract to all nine surfaces (home live hub, leagues/standings/fixtures, seasons/workspace, group standings, match detail, real fixtures/predictions, prediction create/sync, draw generate, interactive pick), and replace every swallowing `catch` with an error state naming the failure + retry: `main.jsx:367,699,708,953,973,984`, `PredictionApp.jsx:120`, `RealDrawView.jsx:263-264` (stop collapsing to a static label). AC US:four-state-contract, US:no-silent-failure, US:loading-preserves-layout. ✓ force pending/empty/failing/succeeding per surface; a 500 shows retry copy and retry re-issues only that request. RB fetch-site edits. All nine surfaces converted. Browse/workspace half: `main.jsx`, `views/TeamsBrowser.jsx`, `views/TeamPage.jsx`, `views/Homepage.jsx`. Prediction half: `PredictionApp.jsx` (create POST -> skeleton / named error + retry; one shared copy for the manual save and the 30s auto-sync; playoffs and knockout failures name themselves and retry their own writer), `views/RealDrawView.jsx` (fixture skeleton, named error + retry replacing the static "Real fixtures unavailable", EmptyState for a fixture-less matchday, real-predictions PUT gets in-flight/named/retry states; a 400 is a closed batch, no retry by design), `main.jsx` draw generate (named error + retry re-issuing only the draw POST; the animation stage is the running state), `views/InteractiveDraft.jsx` (pick gets a one-writer-in-flight guard, busy region + in-flight copy, named error + retry repeating only that pick), `views/MatchDetailView.jsx` (error card gained the missing retry via `ErrorState`; loading was already a dimensioned skeleton with `aria-busy`). `lib/api.js` gained `failure.status` (additive) so the by-design 400 can be told from a real failure. 51 headless-Chromium checks on the built `static/ui/` with `/api/**` stubbed: every retry re-issues only its own endpoint; one draw POST per completed draw with the reveal stepping 0,8,16,...; one pick POST with 36/36 rows disabled in flight and the reveal stepping 0,1,2,3; INV6 focus return after Escape. `python manage.py test` -> Ran 100 tests, OK. `npm run build` OK.
- [x] 4.2 Live-poll failures (incl. 502) render as the live region's inline error state only — never a page-level error, no `aria-live` firing per poll tick; successful polls update in place without moving scroll or focus. AC US:polling-502-contract, US:success-live-refresh; INV5. ✓ stub 502 + new scores while scrolled; rest of page stays usable. RB poll handlers. — `App` keeps the failure in `liveScoresError`; `Homepage` renders one always-mounted `LiveRegion` in a height-reserved slot. Headless Chromium on built `static/ui/` (poll clamped to 350ms): 502 → region text carries the failure, `[role="alert"]`/`.state-error`/`.message-bar.error` all 0, region node identical before/during/after, `scrollY` 900→900 and `activeElement` (quick-action button) unchanged, MutationObserver 0 records across 12 further failing ticks, hero opener still opens the overlay. Recovery with new scores → 2:1 in place, same node, same scroll/focus.
- [x] 4.3 Disable controls that would duplicate an in-flight write; entered values survive a failed submit. AC US:error-writing-inflight. ✓ fail `POST /predictions/{id}/sync/` after typing. RB submit handlers. — one `writeInFlight` ref gates both the manual save and the 30s auto-sync; `isSaving={savingMatchday || syncing}`. Held sync after typing 4/3: Save disabled ("Saving..."), three duplicate attempts → 1 POST; 502 → "Save failed: Request failed with 502. Your scores are still here — use Save Matchday to retry.", inputs still 4/3, matchday still 1, Save re-enabled. While the auto-sync was in flight the same control was disabled and a forced click added no POST (1→1); no new `localStorage` keys.
- [x] 4.4 Raise every interactive control to ≥44×44 (`.md-dot` 32, `button`/`select` 38, `.view-tabs` 32, `.score-input` 34, knockout `ScoreInput` 22, `.hub-open-match` 28, `.picked-chip` ~26). AC A11Y:44px-targets. ✓ measured pass at 390px. RB CSS size rules.
- [x] 4.5 Label form controls: `ScoreInput.jsx` gains a visible-or-visually-hidden name naming team + match; `.md-dot` stops relying on `title`; `HomeMatchCard` (main.jsx:169) becomes a real button/link with a name. AC A11Y:labelled-controls. ✓ accessible-name query + Enter/Space activation. RB `ScoreInput.jsx`, `HomeMatchCard`.
- [x] 4.6 Every status carries text/icon: `.status-dot` in `PlayersRuns` gains a cue; pills use ink-on-fill; markers stay distinguishable in monochrome. AC A11Y:status-not-color-only, A11Y:aa-contrast. ✓ monochrome + computed contrast pass. RB status components/CSS.
- [x] 4.7 Skip link as first focusable → main; global `:focus-visible` (styles.css:1843) survives and the five hardcoded `rgba(21,92,255,…)` rings use `--focus-ring-color`. AC A11Y:skip-link-landmarks, A11Y:keyboard-focus, DT:no-visual-literals. ✓ Tab-once pass + ring visible on every control per theme. RB shell markup + focus CSS.
- [x] 4.8 Match-detail overlay: named dialog, Escape closes, background non-focusable, focus returns to the opener via `detailReturnFocusRef` + `hidden` wrapper (main.jsx:1138). AC A11Y:dialog-focus-return; INV6. ✓ open/tab/Escape/focus-return keyboard pass. RB overlay implementation.
- [x] 4.9 Reduced-motion alternatives for drawer, theme transition and reveals; existing `aria-pressed`/`role="status"`/`aria-busy` unchanged. AC A11Y:reduced-motion-baseline. ✓ `prefers-reduced-motion: reduce` emulation + aria-attribute scan. RB motion CSS. — Only the `@media (prefers-reduced-motion: reduce)` block grew: decorative drops `animation: none` (`.draw-pulse`, `.score-flash`, `.hub-live`, `.hub-score-live`, `.fixture-row .fx-status.live`, `.match-detail-score.live`, `.match-detail-pill.live`, `.match-detail-skeleton .md-skel`), the four reveals keep their end state without playing (`.animated-pot`, `.animated-matchday .fixture-row`, `.reveal-badges .team-logo`, `.picked-chip` → `animation: none; opacity: 1; transform: none`), and the backstop gained `animation-delay: 0s` + `transition-duration: 0s` (was `0.01ms`). Drawer rule from 3.2 untouched. Headless Chromium on the built `static/ui/` with `/api/**` stubbed: computed `animation-name` `hub-pulse|live-pulse|drawPulse|scoreFlash|revealPot|placeFixture|opponent-reveal|pick-fade → none`; pot stagger `animation-delay 0.09s → 0s`; `transition-duration 1e-05s → 0s` everywhere incl. the theme toggle (`background, color`); reveals settle at `opacity 1` / `transform none` (no information lost). Same elements with motion enabled keep `hub-pulse 1.6s infinite`, `mobile-nav-in 0.18s`, `revealPot 0.36s`, `pick-fade 1s`, theme toggle `0.15s, 0.15s` — suppressed, not deleted. aria scan identical before/after and across both motion modes: `aria-pressed` (theme toggle `false→true` on swap; 13 workspace segmented buttons), `role="status"` + `aria-live` (live region, skeleton `sr-only`, `match-detail-sr`), `aria-busy` (skeleton `"true"`, dialog `"true"→"false"`). `npm run build` → `index-Dx-AOPp6.css`. RB `styles.css` reduced-motion block.

## Phase 5 — Career in the shell

- [x] 5.1 `CareerApp.jsx:123` renders as a shell view with shared nav and no `.career-app-shell` chrome; intro/IdentityBuilder/Dashboard/Summary all reachable. AC CS:career-in-shell. ✓ navigate to all four surfaces at 390px and 1440px. RB `CareerApp.jsx`.
- [x] 5.2 Delete `--career-*` (styles.css:2039-2048) and repoint every `.career-*` rule to shell tokens with no compatibility aliases; no gold/navy survives, summary and share output included. AC CS:neutral-hard-replacement, DT:no-visual-literals. ✓ career token/literal scan + dark-theme career render. RB career token block + `.career-*` rules.
- [x] 5.3 Adopt shared primitives: `ClubLogo` (721) retires for `Crest`, `.career-segmented` retires for `SegmentControl`, live `Metric` (489) is re-based, not deleted. AC CS:career-shared-primitives, UC:single-crest. ✓ build + career import scan. RB `CareerApp.jsx` adoption.
- [x] 5.4 Verify engine/storage untouched: `careerEngine.mjs`, `careerClubs.json` and `champions_draw_career_v1` byte-identical in the diff, no new career key; a legacy payload loads with no reset; state survives shell + mobile nav. AC CS:engine-storage-untouched, CS:career-state-survives-nav; INV3. ✓ `npm run test:career` + `git diff --stat` on engine/storage + legacy-payload load. RB revert the Phase-3 nav unit; no career file may change.

## Phase 6 — Reconciliation

- [x] 6.1 RED: write `frontend/src/lib/reconcile.test.mjs` covering reorder, real divergence, missing rows and format drift. AC PR:read-only-semantic-compare. ✓ `node --test frontend/src/lib/reconcile.test.mjs` fails first (reconcile.js absent → `ERR_MODULE_NOT_FOUND`, `pass 0 / fail 1`). 11 tests: standings reorder / divergence / client-only row / server-only row / format drift; playoffs reorder / changed winner / one-sided tie; knockout reorder / changed score / missing slot. RB test file only.
- [x] 6.2 GREEN: `lib/reconcile.js` with pure `compareStandings|comparePlayoffs|compareKnockout(client, server) → {ok, mismatches:[{key, clientValue, serverValue}]}`; keys = team id / tie pair / round+slot; order- and format-insensitive. AC PR:read-only-semantic-compare, PR:client-source-of-truth. ✓ same test command passes (`pass 11 / fail 0`). Purity: only import `./teams.js`; scan for fetch/storage/timers/`window`/`document`/`console` empty. RB `lib/reconcile.js`.
- [x] 6.3 `lib/useReconciliation.js`: `GET`-only to `/predictions/{id}/standings|playoffs|knockout/`; triggers = once per surface per mount when a persisted id exists + once after each successful bulk sync; 30s per-surface limit; no-op without `predictionId`; one `console.warn` (surface, endpoint, client value, server value, status) per mismatch; no retry within a revision. AC PR:bounded-triggers, PR:silent-logged-once, PR:read-only-semantic-compare. ✓ 20 edits in 30s ⇒ ≤1 request per surface (1/1/1); no id ⇒ 0 requests; GET-only (reconWrites [] in every scenario); storage+server untouched. RB hook + call sites.
- [x] 6.4 Module cache `Map<surface,{response,at}>`, 30s TTL matching the rate limit, cleared on any successful bulk sync, memory only. AC PR:bounded-triggers. ✓ repeated tab switches issue no extra request (1/1/1 → 1/1/1); remount inside TTL 1/1/1, after TTL 2/2/2. RB cache module.
- [x] 6.5 Offline / timeout / 502 / non-2xx is a no-op: client values still render, no indicator, toast, `aria-live`, analytics, server report or persisted record; ≤1 warn; the next natural trigger retries. AC PR:offline-no-op. ✓ offline + 502 stub while typing. RB hook error path. — Headless Chromium on built `static/ui/`, `/api/**` stubbed (36 full teams incl. `association.code`). **502**: mount GETs `{standings:1, playoffs:1, knockout:1}`; warn counts afterMount/afterTyping/afterSave **3/3/3** (exactly one per failed check), `role="alert"` 0, `.state-error` 0, `aria-live` texts `[]` (no reconciliation mention), no page error, `reconWrites []`; typing 8 inputs kept GETs at `{1,1,1}` while the sheet advanced `0/8 → 4/8 scored` and the standings sidebar re-rendered; a same-30s bulk sync (`POST /sync/` ×1, revision bump) still did **not** retry (`{1,1,1}`); after the 30 s window the next natural trigger (remount) retried once → `{2,2,2}`, warns 6 = 6 failed checks × 1. **Offline** (`route.abort`): `{1,1,1}` unchanged through typing, 3 warns, `role="alert"`/`.state-error` 0, 0 skeletons, no page error. RB hook error path (unchanged).
- [x] 6.6 Protected keys untouched: no reconciliation key; `champions_draw_prediction_{season}_{player}`, `champions_draw_real_prediction_*` and `champions_draw_career_v1` keep their prior shapes. AC PR:protected-keys; INV3. ✓ storage dump after a full cycle. RB verification only. — `localStorage` seeded with a valid career state and prior shapes, then a full cycle (mount, 8 edits, `Save Matchday` bulk sync, four sub-tab switches, view switch). Key set before == after (`champions_draw_player_name`, `champions_draw_prediction_state`, `champions_draw_prediction_1_Harness`, `champions_draw_real_prediction_1_Harness`, `champions_draw_career_v1`); `newKeys []`, `removedKeys []`, `reconcilKeys []`. Shape checks: prediction key top-level key set/value types identical (raw rewrite is the app's own `saveLocal` `lastUpdated`/scores, not reconciliation); `champions_draw_real_prediction_1_Harness` and `champions_draw_career_v1` **byte-identical** (`rawEqual: true`). `reconWrites []`.
- [x] 6.7 Confirm reconciliation never gates/delays/replaces render and tiebreakers are unchanged (`tieUtils.js` untouched). AC PR:client-source-of-truth; INV4. ✓ `node --test frontend/src/tieUtils.test.mjs` + deliberate-mismatch render unchanged. RB verification only. — `frontend/src/tieUtils.test.mjs` **exists**; `node --test` → `tests 10 | pass 10 | fail 0`. `git diff HEAD -- frontend/src/tieUtils.js` empty (byte-identical to `HEAD`). Headless Chromium: deliberate server divergence (team 5 `points: 3`) is detected (1 warn, payload `{key:"5", clientValue.points:0, serverValue.points:3, status:200}`) yet the rendered league table (36 rows) and sidebar (36 rows) text are **identical** to the agreement run (`mismatchSignatureEqual: true`); with the standards GET left hanging the surface is fully rendered at 400 ms and 3000 ms (36/36 rows, 0 skeletons, `hangSignatureEqual: true`) — no delay, blanking or re-render.

## Phase 7 — Verification gates

- [x] 7.1 `npm run build` (root) succeeds into `static/ui` and Django serves it; `python manage.py test` green (draw POST + `summary` shape, endpoints, INV1); `npm run test:career` green. — build exit 0; `python manage.py test` **Ran 100 tests in 41.343s / OK**; `npm run test:career` pass 3 / fail 0. **Verifier blocker fixed in `6add243`**: no commit had included the rebuilt `static/ui/`, so a clean checkout served the pre-change UI (`git diff e95c1a9..d26c141 -- static/ui` was empty). The bundle is now committed and carries `data-theme` ×1 / `--focus-ring-color` ×5 / `--status-ink` ×17 / `career-` ×70 / `mobile-nav` ×31, matching the verifier's correct-rebuild counts.
- [x] 7.2 `node --test frontend/src/lib/reconcile.test.mjs` and `frontend/src/tieUtils.test.mjs` green; static scans clean — zero color/radius/shadow literals outside token blocks (incl. `sharePredictionsImage.js:17-20` reading tokens), every `@media` width in the declared set, dead selectors gone, no banned decoration. ✓ static scans (comments stripped, `:root`+`[data-theme="dark"]` blanked): colour occurrences **82→0**, radius literals **56→0**, shadow literals **9→0**; `@media` widths unchanged `{640,760,920,1120}`; unreferenced classes **390→386** — `.app-shell`/`.table-number`/`.toggle-row`/`.skeleton--real-league` deleted, remaining 8 are dynamic (`badge-${tone}`, `live-region-${tone}`, `skeleton--${variant}`); banned decoration all 0 and the inset stripe `box-shadow: inset 3px 0 0 #1e6fd9` removed — `.team-row.selected` now uses `outline: 2px solid var(--select-ring); outline-offset: -2px`. Both-theme render signatures (headless Chromium, HEAD-source vs worktree-source) identical except **9 intended diffs**: `.team-row.selected` stripe→ring (light+dark) and dark-only `--qualify-fill`/`--playoff-fill`/`--eliminated-fill`/`--state-message-bg` counterparts plus `.hub-open-match:hover` (`--soft-blue` reuse). `node --test` reconcile 11/11, tieUtils 10/10; `npm run build` OK; built `static/ui/` boots with 0 page errors. RB `styles.css` token blocks + replaced rules.
- [x] 7.3 Computed contrast matrix passes AA in both themes for every text/status/control pair (no pill under 3:1), and a keyboard-only browser pass at 390/920/1120/1440 covers drawer trap, Escape, focus return, both themes and reduced motion. ✓ Re-run on the rebuilt `static/ui/` (headless Chromium, `/api/**` stubbed): **195 unique text pairings, 0 fail / 0 under 4.5** in both themes. Fixes at token level: `.view-tabs`/`.segment-control` active → `--green-ink`; `.gd-pos` → `--green-ink`; `.gd-neg`/`.verdict-miss` → `--red-ink`; `.verdict-exact` → `--green-ink`; `.verdict-outcome` → new `--amber-text` (light/dark); `.versus` → new `--gold-ink` (light/dark); `.career-kicker`/career `strong` → `--blue-dark`; dark `--select-bg`/`--select-ink` added so `.team-row.selected em` is 5.7:1. `.match-detail-pill` class mismatch fixed (`finished`/`in_play` now match the lowercased status; dead `.live` renamed): Final white/`--slate` 4.76:1, Live `--status-ink`/`--qualify-mark` 5.49:1. Keyboard + reduced motion at 390/920/1120/1440 × light/dark byte-identical to the accepted baseline (skip link, drawer/overlay `:modal` trap, Escape, focus return, `aria-pressed`/`role=status`/`aria-busy`). Colour literals outside token blocks: 0. `npm run build` OK. RB `styles.css` only.
